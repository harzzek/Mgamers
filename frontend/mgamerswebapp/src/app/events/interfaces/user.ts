export interface User {
    id: number
    username: string;
    name: string;
    email: string;
    birthdate: string;
    roles: string[];

    // Add other event properties as needed
  }

  //id, username, name, email, birthdate, roles